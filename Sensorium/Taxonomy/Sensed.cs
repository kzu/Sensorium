namespace Sensorium.Taxonomy
{
    public static class Sensed
    {
        public const string Temperature = "sensed/{device}/temp";
        public const string Movement = "sensed/{device}/moved";
        public const string Light = "sensed/{device}/light";
        public const string PH = "sensed/{device}/ph";
    }
}
