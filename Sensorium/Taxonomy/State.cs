namespace Sensorium.Taxonomy
{
    public static class State
    {
        public const string On = "state/{device}/on";
        public const string Off = "state/{device}/off";
        public const string Sleep = "state/{device}/sleep";
    }
}
